import ComingSoon from "@/components/dashboard/ComingSoon";
import { MdGroupAdd } from "react-icons/md";

export default function InviteEarnPage() {
  return (
    <ComingSoon
      title="Invite & Earn"
      description="Invite friends and earn a bonus for every referral."
      icon={MdGroupAdd}
    />
  );
}
